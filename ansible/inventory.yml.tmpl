---

all:
  hosts:
    # server:
    #   ansible_port: 
    #   ansible_host: 
    u5:
      ansible_port: 
      ansible_host:  
  vars:
    ansible_user: user
    ansible_password: 
    ansible_become_pass: 
    # ansible_ssh_private_key_file: ~/.ssh/ssh_key
